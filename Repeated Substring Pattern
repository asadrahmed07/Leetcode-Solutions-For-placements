//leetcode problem no. 459
class Solution {
    public boolean repeatedSubstringPattern(String s) {
  
        String ss = s + s;
        boolean ans = ss.substring(1 , ss.length()-1).contains(s);
        return ans;
    }
}
